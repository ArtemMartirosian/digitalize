export interface TeamMemberDataProps {
  fullName: string;
  slug: string;
  position: string;
}

export interface TechnologyDataProps {
  technologyName: string;
  image: string;
}

export interface ServiceDataProps {
  name: string;
  price: number;
  duration: string;
}
